import type { TestimonialItem } from "@/components/ui/Testimonial";

// TODO: replace with real attributed quotes — do not ship placeholders.
// The TestimonialGrid renders nothing while this array is empty, so the
// slots on the homepage, /contact, and the cost page stay dark until
// Saren collects real quotes (name + title + company, with permission).
export const TESTIMONIALS: TestimonialItem[] = [];
