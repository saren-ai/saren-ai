Also update:
1. The "Components" section to remove references to deleted directories and add the new `feature/` component directory.
2. The "Libraries" section to remove `thinking.ts` and `substack-rss.ts`, add `feature.ts`.
3. The "Common Tasks" section: replace "Adding a new portfolio case study" with two distinct tasks — "Adding a new portfolio case study" AND "Adding a new feature article."
4. Add a new section called **"IA Conventions"** that codifies the current rules:
   - `/portfolio/*` is for proof-of-work case studies and interactive tools demonstrating capability
   - `/playbooks/*` is for structured, reusable artifacts (frameworks, templates)
   - `/feature/*` is for editorial/personal projects (magazine-style)
   - `/signal-state/*` is the Signal State framework workspace
   - `/halcyon/*` is archived; do not link from primary nav
   - `/brand` is the Fire Horse 2026 brand guideline; `/about/brand` is deprecated and 301'd

---

## Phase 7: Generate and configure sitemap

1. Install `next-sitemap`: `npm install --save-dev next-sitemap`
2. Create `next-sitemap.config.js` at project root:
```js
   module.exports = {
     siteUrl: 'https://saren.ai',
     generateRobotsTxt: true,
     sitemapSize: 7000,
     exclude: [
       '/halcyon',
       '/halcyon/*',
       '/api/*',
     ],
     robotsTxtOptions: {
       policies: [
         { userAgent: '*', allow: '/' },
         { userAgent: '*', disallow: '/halcyon' },
         { userAgent: '*', disallow: '/api' },
       ],
     },
   };
```
3. Add to `package.json` scripts: `"postbuild": "next-sitemap"`
4. Run `npm run build` and confirm `public/sitemap.xml` and `public/robots.txt` are generated.
5. Output a final checklist for Saren to manually complete:
   - Submit sitemap to Google Search Console
   - Submit sitemap to Bing Webmaster Tools
   - Request reindexing of saren.ai root in Search Console (to displace legacy Medium-era results)
   - Verify no critical pages are accidentally excluded

---

## Phase 8: Final verification

1. Run `npm run build`. Confirm zero errors.
2. Run `npm run lint`. Fix any lint errors introduced.
3. Run `npm run dev` and manually verify:
   - `/brand` renders the Fire Horse guideline
   - `/about/brand` 301s to `/brand`
   - `/portfolio/b2b-marketing-framework` 301s to `/playbooks/b2b-marketing-framework`
   - `/feature` renders the index with the Kwannon placeholder
   - `/feature/kwannon-timeline` renders the article template
   - `/halcyon` still works but isn't in the mega menu
   - `/signal-state` is in the mega menu and all sub-routes work
4. Output a summary: what was changed, what redirects were added, what files were deleted, what manual SEO steps remain.

---

## Constraints

- Use the existing Fire Horse 2026 design system. No new colors, fonts, or utility classes.
- All new pages need `"use client"` only if they have client-side interactivity. Default to server components for static content (the feature article template should be a server component).
- Use Next.js `<Image>` for all images. Never raw `<img>`.
- Sentence case headlines throughout. JetBrains Mono for dates/metrics. Sora for body and headings.
- Pause and ask before any decision not explicitly covered above. Do not invent solutions to ambiguous cases.