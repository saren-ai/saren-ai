# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# next.js
/.next/
/out/

# pagefind (generated at build time — do not commit)
/public/_pagefind/

# production
/build

# testing
/coverage

# typescript
*.tsbuildinfo
next-env.d.ts

# env files
.env*

# vercel
.vercel

# OS
.DS_Store
Thumbs.db

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# secrets / keys
*.pem

# local databases
/data/
*.db
*.db-shm
*.db-wal

# editor / IDE
.cursor
.idea
.vscode
!.vscode/settings.json
!.vscode/extensions.json

# Claude Code (worktrees, local settings, and personal overrides are machine-specific)
.claude/worktrees/
.claude/settings.local.json
CLAUDE.local.md

# personal / application materials
halcyon-content/

# obsidian (content vaults checked in, but not config)
**/.obsidian

# ── Prospecting workspace (merged from @lead-prospecting, 2026-06-18) ──
# Client dossiers contain PII / OSINT — keep on disk, never commit to this public repo
/prospecting/clients/
# Agent runtime state
/prospecting/.active-client
/prospecting/.inbox-watch-state.json
# Vendored external sales suite (has its own git history; external, read-only)
/prospecting/vendor/
# Legacy dashboard build artifacts (archived source kept, build output ignored)
/prospecting/legacy-dashboard/node_modules/
/prospecting/legacy-dashboard/.next/
/prospecting/legacy-dashboard/*.tsbuildinfo

# generated markdown snapshot of the live site (drifts from source, regenerate on demand)
/docs/site-export/
